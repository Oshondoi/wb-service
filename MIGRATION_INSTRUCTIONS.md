# Применение миграции operation_type

## Что сделано:

✅ **1. Убраны автокомментарии** 
- Удален префикс "Начисление: " / "Погашение: " из поля `note`
- Теперь комментарий записывается как есть (или остается пустым)

✅ **2. Добавлена колонка "Тип операции"**
- Новая колонка в таблице "Записи" показывает "Начисление" или "Погашение"
- Значение берется из поля `operation_type` в БД

✅ **3. Переименован подраздел**
- "Операции" → "Записи" (чтобы не путать с разделом "Операции" в ДДС)

## Что нужно сделать в Supabase:

1. Откройте Supabase SQL Editor: https://supabase.com/dashboard/project/fjkcpndtirljciaaakpr/sql
2. Выполните SQL из файла `migration-operation-type.sql`:

```sql
ALTER TABLE cash_debts 
ADD COLUMN IF NOT EXISTS operation_type TEXT DEFAULT 'increase';

UPDATE cash_debts 
SET operation_type = CASE 
  WHEN amount < 0 THEN 'decrease'
  ELSE 'increase'
END
WHERE operation_type IS NULL OR operation_type = 'increase';
```

3. Перезапустите сервер (он уже запущен и ждет миграции)

## Измененные файлы:

- `fin-report.js` - убрано добавление префикса в комментарий, добавлен столбец "Тип операции"
- `database.js` - добавлена поддержка поля `operation_type`
- `supabase-schema.sql` - обновлена схема таблицы `cash_debts`
- `migration-operation-type.sql` - SQL для миграции существующих данных

После выполнения миграции сервер заработает полностью! 🚀
