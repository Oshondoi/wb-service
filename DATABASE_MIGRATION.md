# 🗄️ Database Migration & Multi-Company Architecture

## Дата: 7 декабря 2025

## Что изменилось

### 1. 🏗️ Новая архитектура базы данных

Сервис теперь использует **локальную SQLite базу данных** вместо файлового хранилища API ключей.

**Файлы:**
- `database.js` — модуль для работы с базой данных
- `wb-service.db` — файл базы данных (создаётся автоматически)

**Структура БД:**

```sql
-- Таблица аккаунтов
CREATE TABLE accounts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  email TEXT UNIQUE,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Таблица компаний
CREATE TABLE businesses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id INTEGER NOT NULL,
  company_name TEXT NOT NULL,
  wb_api_key TEXT NOT NULL,
  description TEXT,
  is_active INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE CASCADE
);
```

### 2. 🔐 Обновлённая система авторизации

**Удалено:**
- Статические константы `ADMIN_LOGIN` и `ADMIN_PASSWORD`
- Файловое хранилище `wb-api-key.txt`

**Добавлено:**
- Хеширование паролей (SHA256 + соль)
- Авторизация через базу данных
- Токен авторизации = ID аккаунта
- Middleware `requireAuth` проверяет токен и загружает аккаунт из БД

### 3. 🏢 Система управления компаниями

**Новые возможности:**
- Один аккаунт может иметь несколько компаний
- Каждая компания имеет свой API ключ Wildberries
- Активация/деактивация компаний
- Удаление компаний
- Переключение между компаниями в интерфейсе

**Новый UI:**
- Кнопка "🏢 Управление компаниями" в `/fin-report`
- Модальное окно с CRUD операциями
- Селектор компаний в шапке страницы
- Карточки компаний с кнопками управления

### 4. 🔄 Автоматическая миграция данных

При первом запуске сервис:
1. Создаёт дефолтный аккаунт: `admin` / `tarelkastakan`
2. Читает старый файл `wb-api-key.txt` (если существует)
3. Создаёт первую компанию "Моя компания" с этим ключом
4. Переименовывает старый файл в `wb-api-key.txt.backup`

## Новые API endpoints

### Управление компаниями

```javascript
// Получить список компаний текущего аккаунта
GET /api/businesses
Response: { success: true, businesses: [...], stats: {...} }

// Создать новую компанию
POST /api/businesses
Body: { company_name, wb_api_key, description }
Response: { success: true, business: {...} }

// Обновить компанию
PUT /api/businesses/:id
Body: { company_name?, wb_api_key?, description?, is_active? }
Response: { success: true, message: "..." }

// Удалить компанию
DELETE /api/businesses/:id
Response: { success: true, message: "..." }

// Получить компанию по умолчанию
GET /api/businesses/default
Response: { success: true, business: {...} }
```

### Обновлённые финансовые API

Все финансовые endpoints теперь принимают параметр `businessId`:

```javascript
GET /api/wb-finance?businessId=1&dateFrom=2025-01-01&dateTo=2025-12-31
GET /api/wb-sales?businessId=1&dateFrom=2025-01-01&dateTo=2025-12-31
GET /api/wb-orders?businessId=1&dateFrom=2025-01-01&dateTo=2025-12-31
GET /api/wb-sales-grouped?businessId=1&dateFrom=2025-01-01&dateTo=2025-12-31
GET /api/wb-fin-report?businessId=1&dateFrom=2025-01-01&dateTo=2025-12-31
```

Если `businessId` не указан — используется первая активная компания.

## Изменения в коде

### Удалено из index.js:
- `const ADMIN_LOGIN = 'admin'`
- `const ADMIN_PASSWORD = 'tarelkastakan'`
- `let WB_API_KEY = ''`
- `app.post('/api/save-wb-key', ...)`
- `app.get('/api/check-wb-key', ...)`
- Модалка добавления API ключа
- Функции `openApiModal()`, `closeApiModal()`, `saveApiKey()`

### Добавлено в index.js:
- `const db = require('./database')`
- Новый `requireAuth` middleware с проверкой через БД
- API endpoints для управления компаниями
- Обновлённые финансовые endpoints с `businessId`
- Новая модалка управления компаниями
- Функции `openBusinessManager()`, `loadBusinesses()`, `addBusiness()` и др.
- Селектор компаний в UI

## Установка новой зависимости

```bash
npm install better-sqlite3
```

## Безопасность

1. **Пароли**: Хешируются с использованием SHA256 + соль (pbkdf2, 1000 итераций)
2. **SQL Injection**: Все запросы используют prepared statements
3. **Авторизация**: httpOnly cookies, работают на serverless
4. **Валидация**: Проверка принадлежности компаний к аккаунту перед операциями
5. **CASCADE DELETE**: При удалении аккаунта автоматически удаляются все его компании

## Масштабирование

### Преимущества новой архитектуры:
✅ Множество компаний на один аккаунт
✅ Централизованное управление API ключами
✅ Безопасное хранение паролей
✅ Простое расширение: можно добавить новые поля в таблицы
✅ SQL запросы для аналитики и отчётов

### Ограничения:
⚠️ SQLite — локальная БД (не подходит для распределённого serverless)
⚠️ Для продакшена на масштабе рекомендуется PostgreSQL/MySQL
⚠️ Нет механизма репликации и бэкапов (нужно настроить вручную)

## Как пользоваться

1. Запустить сервер: `npm start`
2. Зайти на `/login` (admin / tarelkastakan)
3. Перейти на `/fin-report`
4. Нажать "🏢 Управление компаниями"
5. Добавить компании с разными API ключами
6. Переключаться между компаниями через селектор
7. Загружать финансовые отчёты для каждой компании отдельно

## Откат к старой версии

Если нужно вернуться к файловой системе:
1. Восстановить `wb-api-key.txt.backup` → `wb-api-key.txt`
2. Откатить index.js к предыдущему коммиту
3. Удалить `database.js` и `wb-service.db`
4. Удалить `better-sqlite3` из package.json

## Тестирование

Протестировано:
✅ Миграция из старого файла
✅ Создание аккаунта и компаний
✅ Авторизация через БД
✅ CRUD операции с компаниями
✅ Загрузка финансовых данных по компаниям
✅ Переключение между компаниями
✅ Удаление компаний
✅ CASCADE DELETE при удалении аккаунта

## Документация

Обновлены файлы:
- `README.md` — описание новой архитектуры
- `memory-bank/activeContext.md` — последние изменения
- `memory-bank/techContext.md` — технические детали
- `memory-bank/systemPatterns.md` — паттерны и принципы
- `memory-bank/projectbrief.md` — цели и скоуп проекта
