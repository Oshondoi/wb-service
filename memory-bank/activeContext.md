# Active Context

## Latest Session (Mar 4, 2026) - ✅ SHIPMENTS-2 SOURCE-FIRST FLOW + PRODUCT STEP REWORK + MGMT MODALS

**🎯 CURRENT FOCUS**: Довести `/shipments-2` до полного операционного сценария создания партии: явный выбор источника, рабочий шаг добавления товаров, отдельные модалки управления источниками/складами и финальная UX-полировка скроллов/таблиц.

### Что сделано в текущей сессии:
- Wizard создания партии переведён на 5-шаговый flow с первым шагом выбора источника:
  - `Этап 1/5` — источник,
  - `Этап 2/5` — компания,
  - `Этап 3/5` — количество складов,
  - `Этап 4/5` — выбор складов,
  - `Этап 5/5` — добавление товаров.
- На шаге товаров реализован прикладной сценарий добавления позиции: штрихкод (ручной ввод/камера), склад, короб, количество, добавление в список.
- Выбор коробов сделан зависимым от выбранного склада.
- Добавлена автологика: если пользователь вводит номер короба, которого нет в выбранном складе, короб создаётся автоматически и товар добавляется в него.
- Кнопка «Обновить» заменена на **«Создать источник»** с целевым поведением создания сущности.
- Созданы отдельные модалки управления:
  - источниками (создание + список + удаление),
  - складами (создание + список + удаление).
- Удалены вспомогательные подсказки из wizard по запросу пользователя; интерфейс оставлен минимальным.
- Обновлён layout шага товаров:
  - полосы выбора складов/коробов вынесены в горизонтальные скролл-области,
  - область списка добавленных позиций сделана вертикально прокручиваемой,
  - скорректированы отступы и порядок колонок в таблице (партия/название поставки).
- Для горизонтальных списков складов и коробов зафиксировано принудительное отображение scrollbar даже при малом количестве элементов.

### Проверки:
- `/shipments-2` после серии правок стабильно отдаёт `HTTP 200`.
- Диагностика по `src/services/shipments2.service.js` после последних изменений — без ошибок.

**Статус:** ✅ Flow `/shipments-2` доведён до требуемого UX и готов к дальнейшей оптимизации скорости сохранения.

## Latest Session (Mar 4, 2026) - ✅ SHIPMENTS-2 WIZARD + READ-ONLY DETAILS + ROW CLICK UX

**🎯 CURRENT FOCUS**: Убрать нативные `prompt/dialog` из создания партии в `/shipments-2`, перевести flow на управляемую модалку по шагам и упростить просмотр деталей поставки.

### Что сделано в текущей сессии:
- Полностью заменён prompt-flow «Создать партию» на встроенную модалку-мастер (wizard) с поэтапным UI.
- Добавлен пошаговый сценарий создания:
  - выбор компании,
  - выбор партии,
  - ввод количества складов (только цифры, мобильная numeric-клавиатура, без spinner-стрелок),
  - выбор конкретных складов по заданному количеству.
- Добавлен `Back`-переход между шагами и стабилизирован footer модалки (кнопки в фиксированных позициях на всех шагах).
- Введено draft-поведение в памяти + защита от случайного закрытия: при попытке закрыть незавершённую модалку показывается подтверждение.
- Для обязательных полей включены явные пустые placeholder-значения, чтобы пользователь явно выбирал бизнес/склад.
- Из модалки удалены вспомогательные тексты по запросу пользователя; оставлен минимальный рабочий интерфейс.
- Изменена бизнес-логика сохранения: **одна партия → несколько поставок** (по одной записи поставки на каждый выбранный склад), вместо одной поставки с множеством складов.
- Модалка редактирования переопределена как **«Детали поставки»** (read-only): поля только для просмотра, без сохранения изменений.
- Открытие деталей перенесено с маленькой иконки на клик по строке таблицы (только зона колонок от даты до автора, без чекбокса и action-иконок).
- Добавлены визуальные подсказки интерактивности: `cursor: pointer` и hover-подсветка для кликабельной зоны строки.

### Проверки:
- `/shipments-2` после изменений стабильно отдаёт `HTTP 200`.
- Диагностика по изменённому `src/services/shipments2.service.js` — без синтаксических ошибок.

**Статус:** ✅ Новый UX `/shipments-2` доведён до согласованного с пользователем поведения, готов к следующей сессии.

## Latest Session (Feb 28, 2026) - ✅ PROFILE PASSWORD RESET FLOW (EMAIL + RECOVERY FORM)

**🎯 CURRENT FOCUS**: Довести до полного UX цикл восстановления пароля из модалки профиля: отправка письма + экран ввода нового пароля после перехода по ссылке.

### Что сделано в текущей сессии:
- В общую модалку профиля (`renderProfileModal`) добавлена кнопка **«Сбросить пароль на почту»**.
- В shared-скрипт профиля добавлен вызов `POST /api/profile/reset-password` с текущим email.
- Backend endpoint `POST /api/profile/reset-password` отправляет письмо через Supabase `resetPasswordForEmail`.
- Добавлен `redirectTo` на `'/auth?mode=recovery'`, чтобы ссылка из письма вела в recovery-режим приложения.
- Реализован recovery-flow на странице `/auth`:
  - отдельная форма **«Новый пароль / Повторите пароль»**,
  - парсинг recovery-токена из URL hash,
  - отправка нового пароля в `POST /api/auth/reset-password-confirm`.
- Добавлен публичный endpoint `POST /api/auth/reset-password-confirm`, который проверяет recovery-токен и обновляет пароль пользователя через Supabase Admin API.

### Важно для эксплуатации:
- В Supabase (`Authentication -> URL Configuration`) обязательно добавить `http://localhost:3000/auth?mode=recovery` в **Redirect URLs**.
- Без этого recovery-письмо может возвращать пользователя на базовую страницу без формы установки нового пароля.

**Статус:** ✅ Полный сценарий «сбросить пароль → письмо → установить новый пароль» реализован на стороне приложения.

## Latest Session (Feb 27, 2026) - ✅ SHIPMENTS-2 JS FIX + DEBTS TABLE COLUMNS

**🎯 CURRENT FOCUS**: Восстановить корректный рендер `/shipments-2` после JS-сбоя и донастроить порядок колонок в разделе «Долги».

### Что сделано в текущей сессии:
- В `/shipments-2` исправлена критичная ошибка inline-скрипта (`Invalid or unexpected token`) — проблема была в неэкранированном переводе строки `\n` в функции создания партии.
- В тулбар `/shipments-2` добавлена кнопка **«Создать партию»** (левая позиция среди action-кнопок).
- В `Долги -> Список контрагентов` добавлена колонка **«Магазин»** и затем размещена в целевом порядке: **между «Прогресс» и «Всего»**.
- В `Долги -> Записи` добавлена колонка **«Создана»** между **«Комментарий»** и **«Действия»** с форматом даты/времени как в разделе `Движение`.
- Актуализированы `colspan` для состояний «Загрузка/Нет записей» после добавления новых колонок.

**Статус:** ✅ `/shipments-2` рендерится стабильно; таблицы долгов обновлены по целевому UX.

## Latest Session (Feb 24, 2026) - ✅ SHIPMENTS-2: SYSTEM WAREHOUSES + SIZE PARITY WITH MAIN PAGE

**🎯 CURRENT FOCUS**: Зафиксировать финальную конфигурацию `/shipments-2` после серии UX-итераций: правила складов, формат отображения и приведение размеров элементов к главной странице.

### Что зафиксировано в финале:
- В `/shipments-2` добавлена цветовая индикация статусов поставки:
  - `draft` → жёлтый,
  - `done` → зелёный,
  - `canceled` → красный.
- Таблица `/shipments-2` расширена и стабилизирована по порядку колонок: между `Партия` и `Кол-во коробов` добавлен `Склад`.
- Удалён промежуточный UX с чекбоксом «Показывать англ.»; отображение складов теперь всегда в едином EN/RU-варианте.
- В тулбаре оставлена модель действий через кнопку **«Создать склад»** (вместо фильтра складов).
- Введена политика системных складов: фиксированный глобальный набор, системные названия нельзя создать вручную и нельзя удалить.
- API списка складов в `/shipments-2` гарантирует наличие системных складов для аккаунта (автодозаполнение при чтении).
- Приведены размеры элементов `/shipments-2` к метрикам главной страницы:
  - чекбокс-колонка `32px`,
  - нативный размер чекбоксов,
  - компактные тулбар-кнопки (`padding 6x10`),
  - выровненные метрики dropdown-меню.
- Название страницы в контенте «Отгрузки 2» убрано; сохранён формат экрана в стиле остальных разделов.

**Статус:** ✅ `/shipments-2` приведён к актуальным бизнес-правилам складов и визуально выровнен по размерностям с главной страницей.

## Latest Session (Feb 24, 2026) - ✅ MIGRATION WRAP-UP: FILE SPLITTING COMPLETED END-TO-END

**🎯 CURRENT FOCUS**: Зафиксировать полный итог работ, начатых с первого шага по дроблению монолитных файлов и распределению кода по модульной структуре.

### Что зафиксировано в финале:
- Подтверждён полный переход на модульный runtime: `index.js -> src/server.js -> src/app.js`.
- Legacy-ветка завершена: `fin-report.js` удалён, runtime-зависимостей от него не осталось.
- После удаления legacy восстановлена/проверена auth-цепочка middleware (body parser + cookie/session stack).
- Пройден финальный smoke-check: login (`200`) -> защищённые страницы (`200`) -> logout (`302`) -> post-logout редирект (`302`).
- Удалены временные backup-файлы, использованные как страховка в процессе миграции:
  - `index.js.backup`
  - `fin-report.js.backup`
  - `fin-report.backup.js`
- Подтверждено, что после удаления backup-файлов сервер остаётся рабочим (`/auth -> 200`).

**Статус:** ✅ Цикл миграции от первого шага дробления до финальной очистки завершён и зафиксирован в документации.

## Latest Session (Feb 24, 2026) - ✅ ABSOLUTE FINISH: LEGACY FIN-REPORT REMOVED

**🎯 CURRENT FOCUS**: Завершить миграцию с legacy `fin-report.js` до полного удаления файла без регрессий.

### Что сделано в текущей сессии:
- Подтверждено, что runtime уже использует модульную связку `index.js -> src/server.js -> src/app.js`.
- `fin-report.js` переведён в compatibility-wrapper, затем полностью удалён из репозитория.
- Проверено, что в `.js`-коде не осталось импортов/ссылок на `fin-report.js`.
- Подтверждена работоспособность ключевых маршрутов после удаления legacy-файла.

**Статус:** ✅ Миграция завершена полностью; legacy-файл удалён, модульная архитектура является единственным runtime-путём.

## Latest Session (Feb 24, 2026) - ✅ SHIPMENTS-2 UX + TRUE XLSX EXPORT

**🎯 CURRENT FOCUS**: Дошлифовать UX `/shipments-2` и исправить экспорт «Записей долгов» на настоящий Excel без предупреждений.

### Что сделано в текущей сессии:
- `/shipments-2`: добавлены колонки `Коробов` и `Единиц товара` с серверным подсчётом (`boxes_count`, `items_count`).
- `/shipments-2`: унифицировано отображение пустых значений как `—` (включая 0/пусто для счётчиков).
- `/shipments-2`: колонка `Дата поставки` переставлена сразу после чекбокса и сужена до ширины «по содержимому».
- `/shipments-2`: иконка редактирования теперь открывает модалку редактирования поставки (источник + партия) и сохраняет через `PUT /api/fbo/shipments/:id`.
- Профиль: логин сделан неизменяемым (readonly в UI + сервер игнорирует изменение username, обновляется только email).
- Автор в `/shipments-2` переключён с `ID` на логин пользователя (`created_by_user_login`).
- Регистрация: логин дополнительно валидируется как обязательный на клиенте (trim + ошибка до отправки).
- Экспорт долгов: заменён HTML-псевдо-`xls` на настоящий `xlsx` через backend endpoint `POST /api/cash/debts/export-xlsx`.
- XLSX-экспорт: добавлены аккуратные ширины колонок по содержимому, затем по запросу пользователя убран автофильтр из шапки.

**Статус:** ✅ UX и экспорт доведены до рабочего и визуально чистого состояния; серверные smoke-проверки пройдены.

## Latest Session (Feb 22, 2026) - ✅ BATCH ID FORMAT FIXED (`STORE - N`)

**🎯 CURRENT FOCUS**: Привести ID партий к читаемому формату `Название магазина - номер` во всех точках (создание, API, UI, старые записи).

### Что сделано в текущей сессии:
- В `POST /api/fbo/batches` (`fin-report.js`) генерация `public_id` изменена с числового вида на `${company_name} - ${seq_no}`.
- UI `/shipments` и `/shipments-2` обновлён: убран префикс `ID`, отображается формат `Название - номер`.
- `migration-fbo-scanning-v1_4.sql` обновлён: триггер `fbo_assign_batch_public_id` теперь генерирует `public_id` как `company_name - seq_no`.
- Добавлен SQL-бэкофилл в v1.4 для старых числовых `public_id`.
- Выполнен runtime-бэкофилл существующих партий в БД: старые `public_id` (`1`, `3`, `4`, `5`...) обновлены на формат `name - seq_no`.

**Статус:** ✅ Новые партии создаются как `SS E - N`, существующие записи приведены к тому же формату.

## Latest Session (Feb 22, 2026) - 🔄 FBO OWNER SCOPE + BATCHES (PARTIAL UI)

**🎯 CURRENT FOCUS**: Перевод FBO на иерархию `owner account -> партия -> поставка` с сохранением существующего UX.

### Что сделано в текущей сессии:
- В `fin-report.js` FBO API закреплён на owner scope через `getFboScope()` (для сотрудников используется аккаунт владельца как data owner).
- Добавлены endpoint'ы партий: `GET /api/fbo/batches`, `POST /api/fbo/batches`.
- Создание поставки (`POST /api/fbo/shipments`) теперь поддерживает `batch_id`; при отсутствии назначается/создаётся «Партия по умолчанию».
- Список поставок (`GET /api/fbo/shipments`) возвращает `batch_id` и `batch_name`.
- На `/shipments-2` seed-процедура переведена на owner scope (данные не пишутся на сотрудника).
- На `/shipments-2` отображение названия поставки обновлено: если есть партия, показывается `Партия / public_id`.
- Добавлена новая миграция `migration-fbo-scanning-v1_3.sql` (таблица `fbo_batches`, `fbo_shipments.batch_id`, обновление валидации `fbo_check_shipment_refs`).

**Статус:** ✅ Код и миграция подготовлены, синтаксис проверен; ⏭️ требуется применение SQL v1.3 в Supabase и smoke-тест FBO сценария.

## Latest Session (Feb 20, 2026) - ✅ SHIPMENTS-2 + ДОЛГИ: ЭКСПОРТ/FILTERS + ПРОФИЛЬ LOGIN/NAME

**🎯 CURRENT FOCUS**: Сравнительный UX для отгрузок и усиление управляемости в «Записях долгов» без изменения бизнес-сущностей.

### Что сделано в текущей сессии:
- Добавлена страница **`/shipments-2`** как альтернативный интерфейс сравнения для отгрузок в стиле проекта.
- Для `/shipments-2` сохранена текущая модель данных и логика именования поставок (`public_id` с fallback на `#id`), без внедрения нового алгоритма имен.
- В таблицу `/shipments-2` добавлены: чекбоксы, дата поставки, автор (`created_by_user_id`) и action-иконки.
- API `/api/fbo/shipments` расширен полем `created_by_user_id` для отображения автора в UI.
- Добавлена тестовая связка данных для визуальной проверки: источник + склад + поставка + связь поставка-склад (для account 1).
- Унифицирован профиль: поле **«Логин»** отделено от **«Имя»**, языковое поле убрано; используется единый shared-рендер `renderProfileModal()` + `renderProfileScript()`.
- В «Записях долгов» добавлена кнопка **«Скачать Excel»** рядом с массовым удалением.
- Добавлены фильтры **«Все контрагенты»** и **«Все магазины»** в тулбаре «Записей долгов».
- Исправлен критичный фронтовый регресс (пустой экран/ломался выбор периода): убраны хрупкие inline `onclick`-строки, меню фильтра контрагентов переведено на безопасные `data-*` + `addEventListener`.

**Статус:** ✅ Функции доступны, UX восстановлен после регресса, сервер подтверждён на `localhost:3000`.

## Latest Session (Feb 19, 2026) - ✅ МОДАЛКА ПРОФИЛЯ ИСПРАВЛЕНА НА /shipments

**🎯 CURRENT FOCUS**: Держать один и тот же UX профиля на всех страницах, включая `/shipments`.

### Что сделано в текущей сессии:
- Подтверждена проблема по скриншотам: на `/shipments` контент «Настройки профиля» отображался inline внизу страницы вместо overlay.
- Найдена корневая причина: в CSS шаблона `/shipments` отсутствовали базовые стили модалки (`.modal`, `.modal-content`, `.modal-header`, `.close-btn`) и профильной сетки (`.profile-*`).
- Добавлены недостающие стили непосредственно в route `/shipments` без изменения API/JS-логики.
- Подтверждена архитектура: используется единый рендер модалки через `renderProfileModal()` + единый скрипт `renderProfileScript()` (`openProfileModal()`), а не отдельные разные реализации.

**Статус:** ✅ Поведение модалки профиля снова консистентно: единая точка входа из сайдбара и одинаковый overlay-рендер.

## Latest Session (Feb 16, 2026) - ✅ ЕДИНАЯ МОДАЛКА ПРОФИЛЯ НА ВСЕХ СТРАНИЦАХ

**🎯 CURRENT FOCUS**: Сделать поведение профиля одинаковым и интуитивным на всех роутингах.

### Что сделано в текущей сессии:
- Подтверждена проблема: на `/products` блок «Настройки профиля» рендерился как обычный контент страницы, а не как overlay‑модалка.
- Найдена корневая причина: в CSS шаблона `/products` отсутствовали базовые стили модалок (`.modal`, `.modal-content`, `.modal-header`, `.close-btn`).
- Добавлены недостающие стили модалки в страницу `/products` без изменения бизнес‑логики.
- Проверено, что вызов по иконке профиля остаётся единым (`openProfileModal()`) через общий сайдбар.
- Сервер перезапущен, доступность фронта подтверждена на `localhost:3000`.

**Статус:** ✅ UX профиля унифицирован: одна точка входа и ожидаемое модальное поведение на всех страницах.

## Latest Session (Feb 13, 2026) - ✅ ДОЛГИ: РАЗДЕЛЕНИЕ ПО МАГАЗИНУ ПОДТВЕРЖДЕНО

**🎯 CURRENT FOCUS**: Зафиксировать корректную бизнес-логику группировки долгов в «Списке контрагентов».

### Что сделано в текущей сессии:
- Подтверждена и принята логика разделения долгов по ключу: **контрагент + тип долга + магазин (`business_id`)**.
- Подтверждено, что долг **без магазина** и долг **с магазином** — это разные потоки и могут отображаться отдельными строками.
- Проверено, что механизм взаимозачёта противоположных типов долгов сохранён и не изменён.
- Сервер перезапущен, фронт проверен пользователем: текущее поведение соответствует ожидаемой бизнес-модели.

**Статус:** ✅ Логика утверждена пользователем, изменений в UX не требуется.

## Latest Session (Feb 9, 2026) - ✅ ВЫХОД: ХОВЕР В ЦВЕТЕ + СТАБИЛЬНЫЙ СТАРТ

**🎯 CURRENT FOCUS**: Выровнять стиль кнопки выхода и убрать проблемы с множественными серверами.

### Что сделано в текущей сессии:
- Кнопка **Выход**: ховер‑эффект как у остальных пунктов меню, но с красным цветом.
- Иконка выхода получила красный оттенок в обычном состоянии.
- Уточнена причина “не обновляется после Ctrl+C”: активные фоновые процессы `node`.
- Практика запуска: перед стартом чистить `node` процессы и запускать один сервер на 3000.

**Статус:** ✅ Реализовано, проверить визуально на всех страницах.

## Latest Session (Feb 8, 2026) - ✅ ЛЕВОЕ САЙДБАР-МЕНЮ

**🎯 CURRENT FOCUS**: Перевод верхней навигации в левое вертикальное меню.

### Что сделано в текущей сессии:
- Добавлен единый левый сайдбар на всех страницах (/, /fin-report, /products, /stocks).
- Пункты меню: **Главная**, **Финансовый отчет**, **Анализ товаров**, **Управление остатками**.
- Сайдбар полностью на высоту экрана, без скруглений, с фоном под стиль проекта.
- Иконки меню — монохромные SVG, подписи под иконками; уменьшены размеры элементов.
- Верхний брендовый блок убран из шапок; верхний значок перемещен в меню.
- Кнопка **Выход** перенесена в подвал сайдбара и выделена красным акцентом.
- Добавлена страница-заглушка **`/stocks`**.
- Перенесены кнопки управления магазинами и синхронизации в контейнер заголовка финансового отчета.
- Кнопка периода на главной странице перенесена в строку вкладок «Движение / Долги / Запасы» и выровнена справа.
- На странице «Анализ товаров» удален инфоблок «Максимальная версия».
- Страница «Управление остатками» заполнена рабочей таблицей остатков WB по API ключам.
- Добавлен фильтр магазинов, обновление, отображение себестоимости и суммы по остаткам.
- В таблице остатков настроены фиксированные ширины колонок и скрываемые детали по кнопке «+».
- Таблица занимает всю ширину контейнера, без растягивания фиксированных колонок.

**Статус:** ✅ Реализовано, требуется финальный просмотр UI.

## Latest Session (Feb 7, 2026) - ✅ AUTH PAGE + SUPABASE SIGNUP

**🎯 CURRENT FOCUS**: Единая авторизация и регистрация через Supabase Auth.

### Что сделано в текущей сессии:
- Страница авторизации объединена в `/auth` с вкладками **Вход / Регистрация**.
- `/login` и `/register` теперь редиректят в `/auth`.
- Регистрация через Supabase Auth (email + пароль) с обязательным подтверждением email.
- Логин поддерживает email (Supabase) и legacy логин по username.
- При входе через Supabase создаётся локальный аккаунт (accounts) для связи с данными.
- Обновлён заголовок сервиса на **«Elestet сервис»**.
- UI авторизации: фиксированная высота формы, плавный переход вкладок, отдельный фон для переключателя, убран hover‑движение у вкладок.
- Сообщения об ошибке/успехе показываются поверх страницы, по центру и ближе к верхнему краю.

**Статус:** ✅ Реализовано, требуется стабильный перезапуск сервера.

## Latest Session (Feb 5, 2026) - ✅ ЗАПАСЫ + СКРОЛЛ + ПОДВКЛАДКИ

**🎯 CURRENT FOCUS**: Расширение «Запасов» и единый UI‑скролл.

### Что сделано в текущей сессии:
- Раздел «Остатки» переименован в **«Запасы»**.
- В «Запасах» добавлены новые подвкладки:
  - **В производстве**
  - **Закупки**
  - **В логистике**
  - **Аутсорс услуги**
- Обновлена логика переключения подвкладок (универсальная мапа, сохранение выбора).
- Добавлен единый кастомный скролл на всех страницах (тонкий, в стиле проекта).
- Вертикальный скролл зарезервирован всегда, чтобы убрать «прыжки» контента.

**Статус:** ✅ UI секции добавлены, стили сохранены.

## Latest Session (Feb 5, 2026) - ✅ ПЕРЕРАСЧЁТ ДОЛГОВ (ВЗАИМОЗАЧЁТ)

**🎯 CURRENT FOCUS**: Взаимный зачёт долгов одного контрагента через модалку.

### Что сделано в текущей сессии:
- В «Списке контрагентов» добавлена кнопка перерасчёта (↔) в строке долга.
- Перерасчёт доступен, когда у контрагента есть **оба типа** открытых долгов.
- Модалка показывает остатки по типам, максимум и превью «после перерасчёта».
- Режимы: **авто‑перерасчёт (максимум)** через чекбокс и ручной ввод суммы.
- Авто‑режим заполняет сумму максимумом, ручной очищает поле.
- Создаются **две записи погашения** (decrease) в обеих группах долга.
- Добавлен API endpoint: **`/api/cash/debts/recalculate`**.

**Статус:** ✅ Реализовано, требуется перезапуск сервера для применения.

## Latest Session (Feb 3, 2026) - 🚚 ОСТАТКИ: UI + API + МУЛЬТИВЫБОР МАГАЗИНОВ

**🎯 CURRENT FOCUS**: Раздел «Остатки» в ДДС (два подвида) с таблицей и интеграцией WB.

### Что сделано в текущей сессии:
- Добавлен третий раздел **«Остатки»** рядом с «Движение» и «Долги».
- Внутри «Остатков» добавлены подвкладки:
  - **По API ключам** (данные WB)
  - **У себя на складе** (ручной учёт, таблица‑заглушка)
- Таблица остатков в стиле операций ДДС (sticky header, тёмный стиль):
  - На складе, в пути к клиенту, в пути от клиента, итог
  - Себестоимость и сумма (по `product_costs`)
- Добавлен мультивыбор магазинов для фильтрации таблицы (dropdown как в «Записях» долгов).
- Кнопка **«Себестоимость»** вынесена в тулбар подвкладок и открывает существующую модалку.
- Добавлен API endpoint: **`/api/wb-stocks`** (подтягивает остатки WB по API ключам).
- Сохранение активной подвкладки «Остатки» в `localStorage`.

**Статус:** ✅ UI и API подключены, таблица рендерится, фильтр по магазинам активен.

---

## Latest Session (Feb 3, 2026) - ✅ WB API v4 + PRODUCTS UI + IMAGE PREVIEW

**🎯 CURRENT FOCUS**: Восстановление получения данных по артикулам и улучшение UI /products.

### Что сделано в текущей сессии:
- Переключён WB card API на **v4** (v2/v1 возвращали 404).
- Обновлён парсинг ответа: `products` теперь на верхнем уровне.
- /products приведён к стилю ДДС (шапка, кнопки, секции, таблица).
- `mainImage` теперь через `/wb-image`.
- `/wb-image` перебирает basket‑хосты 01–40 + кэширует найденный домен.
- Добавлен hover‑preview изображения и корректный placeholder «Нет фото» только при ошибке загрузки.

**Статус:** ✅ Реализовано, протестировано локально и задеплоено.

---

## Latest Session (Feb 1, 2026) - ✅ CASHFLOW SUBTABS + BULK ACTIONS + EDIT MODALS

**🎯 CURRENT FOCUS**: UI/UX улучшения ДДС и долгов.

### Что сделано в текущей сессии:
- Добавлены подвкладки в ДДС: **Все / Приходы / Расходы** (фильтрация + сохранение выбора).
- Массовый выбор и удаление:
  - ДДС (операции)
  - Долги: **Список долгов** и **Записи**
  - Новые bulk endpoints: `/api/cash/transactions/bulk`, `/api/cash/debts/bulk`
- Иконки действий вместо текста (монохромные SVG): редактирование/удаление в ДДС и записях долгов.
- В **Списке долгов** кнопка деталей заменена на иконку «лист/док».
- Модалка операций долга из **Списка долгов** (без перехода в «Записи»).
- Модалки редактирования:
  - Операции ДДС
  - Записи долгов
  Сохранение только по кнопке **Сохранить**.
- Исправление активной полоски у подвкладок долгов (изолированы классы).

**Статус:** ✅ Реализовано и запущено на http://localhost:3100

---

## Latest Session (Jan 28, 2026) - ✅ SERVER PORT 3100 FOR THIS SESSION

**🎯 CURRENT FOCUS**: Операционный запуск сервера.

### Что сделано в текущей сессии:
- Сервер запущен на **порт 3100** (через `PORT=3100`).
- Зафиксировано требование пользователя: **в этой сессии всегда использовать порт 3100**.

**Статус:** ✅ Сервер доступен по http://localhost:3100

---

## Latest Session (Jan 25, 2026) - ✅ DEBT OVERFLOW LOGIC + SORTING + VISUAL CLOSED STATE

**🎯 CURRENT FOCUS**: Финализация логики долгов (переплата/переворот) и визуальных правил в таблицах.

### Что сделано в текущей сессии:

**1. Логика переворота долга (переплата):**
- Если операция переворачивает знак долга, система:
  - Закрывает текущую группу до нуля
  - Создаёт новую группу с противоположным типом долга на сумму переплаты
- Реализовано через `findOpenDebtGroup()` (возвращает balance) и серверную логику в `/api/cash/debts`

**2. Группировка долгов = `debt_group_id`:**
- Возврат к корректной логике: каждая группа — отдельный долг (архив закрытых сохраняется)

**3. Визуальное отличие закрытых долгов:**
- Приглушение строки, grayscale, серый статус

**4. Сортировка в "Записях" по дате операции:**
- Новые записи сверху (как в разделе "Движение")

**5. Исправление ошибки рендера:**
- Устранён `ReferenceError: key is not defined` в `renderDebtSummary()`

**6. `debt_group_id` теперь создаётся автоматически:**
- В `createCashDebt()` не вставляется `null`, чтобы сработал DEFAULT UUID

**1. Sticky заголовки таблиц в ДДС:**
- Все таблицы в ДДС (Операции, Список долгов, Операции долгов) теперь имеют фиксированные заголовки
- Добавлено `position: sticky; top: 0; z-index: 10` для `.cash-table th`
- Заголовки остаются видимыми при прокрутке вниз

**2. Прогресс-бар в подвкладке "Список долгов":**
- Добавлена новая колонка "Прогресс" между "Тип" и "Всего"
- Визуальная полоска прогресса показывает % погашения долга
- Цвет полоски зависит от типа:
  - 🟢 Зелёная (#22c55e) для "Нам должны" (receivable)
  - 🔴 Красная (#ef4444) для "Мы должны" (payable)
- Размер: 70px × 14px, border-radius: 8px
- Прогресс = (Оплачено ÷ Всего) × 100%
- Обновлены стили `.cash-pill.receivable` и `.cash-pill.payable` под цвета прогресс-бара

**3. КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ - Логика работы с долгами:**
- **Старая логика (ОШИБОЧНАЯ)**: При добавлении операции искался существующий долг и обновлялся → в таблице "Операции" был только 1 долг на контрагента
- **Новая логика (ПРАВИЛЬНАЯ)**: Каждая операция создаёт НОВУЮ запись в БД
  - Начисление: создаётся запись с положительной суммой (+amount)
  - Погашение: создаётся запись с отрицательной суммой (-amount)
  - В комментарий автоматически добавляется префикс "Начисление: " или "Погашение: "
- **"Список долгов"**: Агрегирует данные по контрагентам на фронтенде
  - Суммирует положительные суммы → "Всего"
  - Суммирует абсолютные значения отрицательных сумм → "Оплачено"
  - Остаток = Всего - Оплачено
- **"Операции"**: Показывает ВСЕ записи из БД как есть
  - Положительные суммы отображаются с "+"
  - Отрицательные суммы отображаются с "-"

**Код изменён:**
- `fin-report.js` lines ~1614, ~3068 - добавлено `position:sticky` для заголовков таблиц
- `fin-report.js` lines ~1619-1621 - обновлены стили `.cash-pill` и добавлен `.debt-progress-bar`
- `fin-report.js` line ~1827 - добавлена колонка "Прогресс" в заголовок таблицы "Список долгов"
- `fin-report.js` lines ~2796-2800 - добавлен рендеринг прогресс-бара в `renderDebtSummary()`
- `fin-report.js` lines ~2760-2778 - исправлена агрегация в `renderDebtSummary()` (учёт +/- сумм)
- `fin-report.js` lines ~2860-2900 - полностью переписана функция `addCashDebt()` (создание новых записей вместо обновления)
- `fin-report.js` lines ~2825-2850 - обновлён `renderCashDebts()` для отображения +/- сумм

**Статус:** ✅ Все изменения применены и протестированы

**📝 ВАЖНО ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ:**
- Для старых записей без `debt_group_id` требуется единоразовый SQL апдейт
- Запланировать редактирование операций:
  - Долги: редактирование записей в подвкладке "Записи"
  - ДДС: редактирование операций в разделе "Движение"

---

## Previous Session (Jan 25, 2026) - ✅ UI REFINEMENTS FOR DEBT SYSTEM

**🎯 CURRENT FOCUS**: Финальные правки текстов и поведения форм в системе долгов.

### Что сделано в текущей сессии:

**1. Изменены тексты в форме создания долга:**
- Тип операции: "Наращивание" → **"Начисление"**
- Тип долга: "Нам должны" → **"Должен контрагент"**
- Тип долга: "Мы должны" → **"Должны контрагенту"**

**2. Изменены названия подвкладок:**
- Подвкладка "Долги" → **"Список долгов"**
- Подвкладка "Операции" → без изменений

**3. Исправлено поведение формы ДДС после добавления операции:**
- Дата теперь автоматически устанавливается на сегодня (вместо очистки)
- Все остальные поля (тип, сумма, категория, контрагент, магазин, комментарий) очищаются
- Форма готова для быстрого ввода следующей операции

**Код изменён:**
- `fin-report.js` line ~1776 - тип долга (Должен контрагент / Должны контрагенту)
- `fin-report.js` line ~1784 - тип операции (Начисление / Погашение)
- `fin-report.js` line ~1815 - подвкладки (Список долгов / Операции)
- `fin-report.js` line ~2658 - сброс формы с автоустановкой текущей даты

**Статус:** ✅ Все изменения применены и проверены

---

## Previous Session (Jan 25, 2026) - ✅ ДОЛГОВАЯ СИСТЕМА С ПОДВКЛАДКАМИ И УМНОЙ ЛОГИКОЙ

**🎯 PREVIOUS FOCUS**: Реализация интеллектуальной системы управления долгами с подвкладками.

### Что сделано:

**1. Структура подвкладок в разделе ДОЛГИ:**
- Добавлены кнопки переключения: "Долги" / "Операции"
- Стили для подвкладок (`.cash-sub-tabs`, `.cash-sub-tab`)
- Форма создания долга вынесена наверх (вне подвкладок, всегда видна)
- Функция `switchDebtSubTab(tab)` для переключения между видами

**2. Добавлено поле "Тип операции":**
```html
<select id="cashDebtOperationType">
  <option value="increase">Наращивание</option>
  <option value="decrease">Погашение</option>
</select>
```

**3. Умная логика в функции addCashDebt():**
```javascript
// Поиск существующего долга
const existingDebt = cashDebts.find(d => 
  d.counterparty === counterparty && 
  d.debt_type === debtType && 
  d.status !== 'closed'
);

if (existingDebt) {
  // Обновление суммы
  let newAmount = Number(existingDebt.amount);
  if (operationType === 'increase') newAmount += operationAmount;
  else newAmount -= operationAmount;
  
  // Автозакрытие при полном погашении
  const newStatus = newAmount <= 0 ? 'closed' : 'open';
  
  // PUT запрос для обновления
  await fetch('/api/cash/debts/' + existingDebt.id, { method: 'PUT', ... });
} else {
  // Создание нового долга (только при наращивании)
  if (operationType === 'decrease') {
    alert('Нельзя погасить несуществующий долг');
    return;
  }
  await fetch('/api/cash/debts', { method: 'POST', ... });
}
```

**4. Сводка долгов (renderDebtSummary):**
- Агрегация по контрагентам: всего / оплачено / остаток
- Кнопка "Детали" → переключает на вкладку "Операции"
- Таблица в элементе `#debtSummaryBody`

**5. Исправления UX:**
- Убрана строка `document.getElementById('cashDebtDueDate').value = dateTo;` из initCashRange()
- Поле "Срок" теперь пустое по умолчанию
- Очистка формы включает `cashDebtDueDate`

**Код изменён**:
- `fin-report.js` (строки ~1770-1900) - HTML структура подвкладок
- `fin-report.js` (строки ~2838-2930) - функция addCashDebt() с умной логикой
- `fin-report.js` (строки ~2743-2800) - функции switchDebtSubTab() и renderDebtSummary()
- `fin-report.js` (строка ~1977) - убрана автоустановка даты срока
- `fin-report.js` (строка ~2889) - инициализация подвкладок при загрузке

**Статус**: ✅ Полностью функциональная система управления долгами

**Важно**: 
- Контрагент + Тип долга = уникальный открытый долг
- Все операции обновляют ОДИН долг (не создают дубли)
- Нельзя погасить долг, которого нет в системе

## Latest Session (Jan 25, 2026) - ✅ ОТОБРАЖЕНИЕ ЧАСОВОГО ПОЯСА В ДАТЕ СОЗДАНИЯ

**🎯 CURRENT FOCUS**: Добавление индикации часового пояса для даты создания операций.

### Что сделано:

**1. Добавлен часовой пояс в колонку "Создана":**
- Формат: "дд.мм.гггг чч:мм (GMT+6)" или аналогичное сокращение
- Часовой пояс отображается полупрозрачным (opacity: 0.6)
- Автоматически определяется из настроек браузера/системы

**Код изменён**:
- `fin-report.js` - функция renderCashTransactions(), добавлен timeZoneName в форматирование даты

**Статус**: ✅ Часовой пояс отображается в колонке "Создана"

**Важно**: Все даты используют локальное время браузера — если пользователь в Бишкеке, покажет UTC+6, если в Москве — UTC+3, и т.д.

## Latest Session (Jan 25, 2026) - ✅ СИСТЕМА 4 БАЛАНСОВ В ДДС

**🎯 CURRENT FOCUS**: Внедрение комплексной системы балансов с учётом долгов.

### Что сделано:

**1. Обновлена панель балансов (4 карточки):**
- 💵 **Кассовый баланс** — что реально в кармане (приходы - расходы)
- 🔄 **Нам должны** — дебиторская задолженность (открытые долги receivable)
- 🔄 **Мы должны** — кредиторская задолженность (открытые долги payable)
- 💼 **Чистый баланс** — с учётом долгов (касса + дебиторка - кредиторка)

**2. Обновлена функция updateCashSummary():**
- Расчёт кассового баланса из операций за период
- Расчёт дебиторки и кредиторки из долгов (только со статусом 'open')
- Расчёт чистого баланса

**Код изменён**:
- `fin-report.js` - HTML структура панели балансов (4 карточки), функция updateCashSummary() (обе копии)

**Статус**: ✅ Система балансов полностью функциональна

## Latest Session (Jan 25, 2026) - ✅ РАЗДЕЛЕНИЕ ДАТЫ ОПЕРАЦИИ И ДАТЫ СОЗДАНИЯ

**🎯 CURRENT FOCUS**: Добавление отображения даты создания записи отдельно от даты совершения операции.

### Что сделано:

**1. Добавлена колонка "Создана" в таблицу операций:**
- Заголовок "Дата" изменён на "Дата операции" для ясности
- Добавлена новая колонка "Создана" показывающая `created_at` (дата и время создания записи в системе)
- Формат: "дд.мм.гггг чч:мм" (дата + время)
- Стиль: серый цвет (#94a3b8), мелкий шрифт (12px)

**2. Обновлён рендеринг таблицы:**
- `renderCashTransactions()` теперь показывает и дату операции (`tx_date`) и дату создания (`created_at`)
- Colspan обновлён с 8 на 9 для корректного отображения

**Код изменён**:
- `fin-report.js` - обновлён HTML заголовок таблицы, функция рендеринга операций

**Статус**: ✅ Теперь система регистрирует дату создания операции отдельно от даты совершения

## Latest Session (Jan 24, 2026) - ✅ КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ АВТОРИЗАЦИИ + ДДС ПОЛНОСТЬЮ РАБОТАЕТ

**🎯 CURRENT FOCUS**: Исправление системы авторизации и создание таблиц ДДС в Supabase.

### Что сделано:

**1. Исправлена авторизация:**
- Убран `httpOnly: false` из cookie (JS может читать токен)
- Добавлена функция `getCookie()` для чтения cookie из JavaScript
- Добавлена функция `syncAuthToken()` для синхронизации токена из cookie в localStorage при загрузке страницы
- Исправлен middleware `requireAuth` - теперь для API запросов возвращает JSON с ошибкой 401 вместо редиректа на `/login`

**2. Обработка ошибок создания справочников:**
- В `database.js` функция `createCashTransaction` теперь оборачивает `upsertCounterparty` и `upsertCashCategory` в try-catch
- Если таблицы не существуют, операция всё равно создаётся (с предупреждением в логах)

**3. Создание таблиц ДДС в Supabase:**
- Выполнена SQL миграция для 4 таблиц:
  - `cash_transactions` - операции движения денег
  - `cash_debts` - долги
  - `cash_categories` - категории операций
  - `counterparties` - контрагенты

**4. Проверка всех таблиц:**
- Подтверждено существование всех 11 таблиц в Supabase

**Код изменён**:
- `fin-report.js` - исправлен cookie (httpOnly: false), добавлены функции getCookie() и syncAuthToken(), исправлен requireAuth middleware
- `database.js` - добавлена обработка ошибок в createCashTransaction
- Supabase SQL Editor - выполнена миграция для 4 таблиц ДДС

**Статус**: ✅ ДДС ПОЛНОСТЬЮ РАБОТАЕТ, все таблицы созданы, авторизация исправлена

### Проблемы и решения:
- **Проблема**: 404 ошибка при добавлении операций → **Решение**: Исправлен requireAuth middleware для API
- **Проблема**: Таблица cash_transactions не существует → **Решение**: Выполнена SQL миграция в Supabase
- **Проблема**: Токен недоступен в localStorage → **Решение**: Убран httpOnly + добавлена синхронизация cookie→localStorage

## Latest Session (Jan 23, 2026) - ✅ ДДС стал главной страницей

**🎯 CURRENT FOCUS**: Движение денег вне магазинов + привязка расходов к магазину.

### Что сделано:
- ДДС перенесён на `/` (главная)
- Финансовый отчёт перенесён на `/fin-report`
- Переименован основной файл: `index.js` → `fin-report.js`, добавлен новый `index.js` как точка входа
- Удалена старая кнопка ДДС в шапке фин. отчёта, добавлена кнопка «Главная»

**Код изменён**:
- fin-report.js: маршруты `/` и `/fin-report`
- index.js: новый entrypoint, подключает `fin-report.js`

**Статус**: ✅ Реализовано, готово к использованию

## Latest Session (Jan 23, 2026) - ✅ Полный редизайн календаря + all-time период + исправления отчётов

**🎯 CURRENT FOCUS**: Корректный режим «За всё время» + стабильные суммы карточек + обновлённая модалка выбора периода.

### Что было сделано:

**1. Полный редизайн модалки выбора периода (календарь):**
- Обновлён стиль сетки календаря, заголовков месяцев, цветов активных/неактивных дней
- Новый hover‑эффект, отдельный от активного состояния
- Обновлена левая панель календаря (dark карточки, мягкие границы)

**2. Быстрый период «За всё время»:**
- Добавлена кнопка «♾️ За всё время» в быстрый выбор
- Диапазон ставится на 2019‑01‑01 → сегодня
- Быстрые периоды больше не автоприменяют фильтр (применяется вручную)

**3. Проверка полноты all‑time перед применением:**
- Добавлен endpoint `/api/fin-report-range/:businessId`
- При выборе «За всё время» проверяется минимальная дата в БД
- Если данных за весь период нет — показывается confirm и запускается `/api/sync/:businessId`

**4. Исправлен лимит 1000 строк Supabase:**
- Добавлена пагинация при чтении `wb_sales`, `wb_orders`, `wb_financial_reports`
- Теперь «За всё время» возвращает полный объём данных

**5. Карточки статистики:**
- Карточки больше не перезаписываются отчётами продаж/заказов
- Карточки считаются только по фин. отчёту

**Код изменён**:
- index.js: UI календаря, быстрый выбор, проверка all‑time + sync
- database.js: пагинация Supabase, helper диапазона дат

**Статус**: ✅ Сервер запущен, изменения применены

**🎯 CURRENT FOCUS**: Полный визуальный редизайн без изменения функционала + корректное позиционирование модальных окон.

### Что было сделано:

**1. Полный редизайн интерфейса (главная + товары + логин)**:
- Внедрён новый «строгий корпоративный» dark-dashboard стиль
- Перестроена шапка: бренд-блок, единая панель инструментов, секции
- Обновлены кнопки, карточки, таблицы, типографика и отступы

**2. Исправление модальных окон (позиционирование/ограничения)**:
- Модалки вынесены из `.container`, чтобы родитель не ограничивал размеры
- Модалки центрируются по вертикали и горизонтали
- Размеры модалок ограничены только экраном (`100vw/100vh` с отступом)
- Обновлён overlay и базовые стили `.modal`/`.modal-content`

**Код изменён**:
- index.js: полный CSS редизайн (главная, login, products)
- index.js: новая структура шапки/секций
- index.js: модалки вынесены из контейнера, размеры `calc(100vw - 48px)`

**Статус**: ✅ Применено, сервер перезапускался, интерфейс обновлён

---

## Previous Session (Jan 19, 2026) - ✅ СВОДНАЯ ИНФОРМАЦИЯ ПО ОТЧЁТУ:

**🎯 CURRENT FOCUS**: Модальное окно со сводной информацией по всему финансовому отчёту

### Что было сделано:

**1. СВОДНАЯ ИНФОРМАЦИЯ - Модальное окно поверх отчёта**:
- **Задача**: Добавить кнопку для просмотра агрегированных данных по всему финансовому отчёту
- **Реализация**:
  - Удалена колонка "Инфо" из каждой строки таблицы (было 72 колонки, стало 71)
  - Добавлена кнопка "📊 Сводная информация" в шапку модального окна финансового отчёта
  - Создана функция `showReportSummary()` - показывает агрегированные данные по ВСЕМУ текущему отчёту
  - Модалка открывается ПОВЕРХ финансового отчёта (z-index: 2000)
- **Что показывает**:
  - № отчета, юридическое лицо, период, дата формирования
  - Финансовые показатели: Продажа, К перечислению, Стоимость хранения, Комиссия, Логистика, Штрафы
  - Информация о партнёре (ИНН, офис, итого к оплате)
- **Код изменён**:
  - index.js - добавлен CSS класс `.modal-overlay` с z-index:2000
  - index.js - модалка `reportInfoModal` получила класс `modal-overlay`
  - index.js - функция `showReportInfo(realizationreportId)` переименована в `showReportSummary()` без параметров
  - index.js - colspan изменён с 72 на 71
- **Результат**: ✅ **Одна кнопка сводной информации вместо кнопок на каждой строке**

**PREVIOUS SESSION - ФИНАНСОВЫЙ ОТЧЕТ КАК НА WB**:

### Что было сделано (Previous):

**1. FULL FINANCIAL REPORT STRUCTURE - 71 колонка как на WB!**:
- **Проблема**: Таблица не соответствовала структуре WB - было 38 колонок, нужно 71
- **Исследование**: 
  - Проанализированы скриншоты с сайта WB Partners
  - Изучена Google таблица (Excel экспорт) - 85 колонок
  - **Решение**: Использовать структуру САЙТА WB (71 колонка), не Excel (пользователи привыкли к сайту)
- **Реализация**:
  - Переделаны заголовки таблицы (~71 <th> тег)
  - Обновлена функция `renderFinReportData()` - теперь выводит все 71 колонку
  - Colspan изменён с 38 на 71
- **Колонки включают**: №, Номер поставки, Предмет, Код номенклатуры, Бренд, Артикул, Название, Размер, Баркод, Тип документа, Обоснование для оплаты, Даты, Количество, Цены, Скидки (СПП, промокод, итоговая), кВВ (размеры, проценты), Возмещения, Эквайринг, НДС, К перечислению, Логистика, Штрафы, Хранение, Технические данные (ШК, Srid, КИЗ, короба, партнеры, склады)
- **Код изменён**: 
  - index.js lines 1451-1530 - полная структура заголовков
  - index.js lines 3291-3368 - функция renderFinReportData() с 71 колонкой
- **Результат**: ✅ **Точная копия структуры WB сайта**

**2. WORD WRAP IN HEADERS - Перенос слов в заголовках**:
- **Проблема**: Колонки были слишком широкие из-за `white-space:nowrap`
- **Решение**: 
  - Убран `white-space:nowrap` из всех заголовков
  - Добавлен `word-wrap:break-word`
  - Установлены `min-width` и `max-width` для каждой колонки (от 40px до 190px)
- **Код изменён**:
  - index.js lines 1451-1530 - все 71 заголовок обновлены
- **Результат**: ✅ Компактные колонки с переносом текста как на WB

**3. TEXT ALIGNMENT - Выравнивание заголовков**:
- **Задача**: Все заголовки должны быть выровнены по левому краю
- **Решение**: 
  - Массовая замена `text-align:right` и `text-align:center` на `text-align:left`
  - Применено ко ВСЕМ заголовкам таблиц (финансовый отчет, продажи, заказы)
- **Команда**: PowerShell regex replace на весь index.js
- **Результат**: ✅ Все заголовки выровнены слева

**PREVIOUS SESSION - FULL HISTORY SYNC + DATE FILTERS**:

**4. FULL HISTORY SYNC - Загрузка за всё время!**:
- **Проблема**: Система загружала только последние 90 дней, хотя WB API может отдать всю историю
- **Исследование**: 
  - `/api/v1/supplier/sales` и `/orders` - лимит 90 дней
  - `/api/v5/supplier/reportDetailByPeriod` - **БЕЗ лимитов!** Можно запросить за всё время!
- **Решение**:
  - Добавлена функция `getFullDateRange()` - загружает с 2019 года (начало статистики WB)
  - Изменён `syncFinancialReport()` - теперь использует `getFullDateRange()` вместо `getMaxDateRange()`
  - Sales и Orders остались с лимитом 90 дней (ограничение WB API)
- **Код изменён**: 
  - sync-service.js ~line 57 - добавлена `getFullDateRange()`
  - sync-service.js ~line 252 - `syncFinancialReport()` использует полный период
- **Результат**: ✅ **Загружено 4041 запись для магазина 1 и 5338 для магазина 2 - ВСЯ история!**

**5. TIMEZONE FIX - Время синхронизации по Бишкеку**:
- **Проблема**: Cron был настроен на 3:30 МСК, нужно было 3:30 по Бишкеку (= 00:30 МСК)
- **Решение**: 
  - Изменён cron с `'30 3 * * *'` на `'30 0 * * *'`
  - Обновлены все сообщения: "3:30 утра (Бишкек)"
- **Код изменён**:
  - index.js ~line 3604 - изменено расписание cron
  - index.js ~line 3617 - обновлено консольное сообщение
  - index.js ~line 3712 - обновлено сообщение о следующей синхронизации
- **Результат**: ✅ Автосинхронизация теперь в 3:30 утра по Бишкеку (00:30 МСК)

**6. DATE FILTER AUTO-RELOAD**:
- При нажатии "Применить" в календаре автоматически загружаются данные
- `applyDateRange()` ~line 2585 - добавлен вызов `loadFinancialData()`

**7. LOCALSTORAGE FOR DATE RANGE**:
- Период сохраняется в localStorage при применении
- При загрузке страницы восстанавливается сохраненный период
- `applyDateRange()` ~line 2586-2587 - сохранение в localStorage
- `DOMContentLoaded` ~line 2612-2628 - восстановление из localStorage

**8. QUICK RANGE BUTTONS FIX**:
- Кнопки Неделя/Месяц/Квартал/Год теперь сразу применяют период и загружают данные
- `selectQuickRange()` ~line 2521 - автовызов `applyDateRange()`
- Конечная дата = сегодня, чтобы грузить строго выбранный период

**STATUS**: ✅ Система полностью работает! 
- Финансовый отчет точная копия WB сайта (71 колонка)
- ВСЯ история WB загружена для всех магазинов
- При создании нового магазина автоматически загрузится вся история (не только 90 дней)
- Автосинхронизация в 3:30 утра (Бишкек) каждый день
- Все фильтры периода работают и сохраняются в localStorage

---

## Previous Session (Jan 17, 2026) - ✅ DATABASE VALIDATION & LOCALSTORAGE:

**🎯 CURRENT FOCUS**: Валидация колонок финансового отчёта + Добавление памяти выбора магазина

### Что было сделано:

**1. COLUMN VALIDATION (Financial Reports)**:
- Проверка соответствия схемы БД (supabase-schema.sql) с данными WB API
- **Результат**: Схема полностью соответствует! 65 колонок данных в wb_financial_reports
- Удалены 14 лишних колонок из отображения таблицы:
  - dlv_prc, fix_tariff_date_from, fix_tariff_date_to
  - acquiring_percent, payment_processing, srv_dbs
  - assembly_id, is_legal_entity, trbx_id
  - installment_cofinancing_amount, wibes_wb_discount_percent
  - cashback_amount, cashback_discount, cashback_commission_change
- Добавлена недостающая колонка `rid` в отображение
- Обновлён комментарий в supabase-schema.sql: "65 колонок данных + 3 системных" (было "82 колонки")
- **Теперь**: Таблица отображает ровно 69 колонок (точное соответствие с БД)

**2. LOCALSTORAGE FOR SELECTED BUSINESS**:
- При выборе магазина из dropdown → сохранение в `localStorage.setItem('selectedBusinessId', id)`
- При загрузке страницы → восстановление из `localStorage.getItem('selectedBusinessId')`
- Автоматическая загрузка данных для восстановленного магазина
- **Функции изменены**:
  - `loadBusinesses()` — восстанавливает сохранённый магазин перед вызовом updateBusinessSelector()
  - `switchBusiness()` — сохраняет выбранный магазин в localStorage
- **Результат**: Пользователь не теряет выбор магазина при обновлении страницы

**3. CODE CHANGES**:
- index.js ~line 1800: loadBusinesses() восстанавливает selectedBusinessId из localStorage
- index.js ~line 1989: switchBusiness() сохраняет выбор в localStorage
- index.js ~line 3280: удалены лишние колонки из renderFinReportData()
- supabase-schema.sql line 115: исправлен комментарий "65 колонок данных"

**STATUS**: ✅ Сервер запущен, изменения применены, localStorage работает

---

## Previous Session (Jan 14, 2026) - ✅ SUPABASE MIGRATION COMPLETE:

**🎯 MAJOR ACHIEVEMENT**: Полная миграция на Supabase PostgreSQL с автоматической синхронизацией данных WB

### Что было сделано:

**1. MIGRATION TO SUPABASE**:
- Создана схема из 7 таблиц в Supabase PostgreSQL (supabase-schema.sql)
- Полностью переписан database.js: все функции async/await вместо синхронных SQLite
- Установлены зависимости: @supabase/supabase-js, node-cron, dotenv
- Настроен .env с Supabase URL и Service Role Key
- Создан supabase-client.js для инициализации подключения

**2. AUTO-SYNC SYSTEM (sync-service.js)**:
- Cron job: каждый день в 3:30 AM (МСК) запускает `syncAllBusinesses()`
- Функции синхронизации:
  - `syncSales(businessId, wbApiKey)` — загрузка продаж за 90 дней
  - `syncOrders(businessId, wbApiKey)` — загрузка заказов за 90 дней
  - `syncFinancialReport(businessId, wbApiKey)` — загрузка полного фин. отчёта (82 колонки)
  - `syncAllData(businessId, wbApiKey)` — запуск всех трёх синхронизаций
  - `syncAllBusinesses()` — синхронизация всех активных магазинов
- Batch insert по 500 записей для производительности
- Логирование в sync_logs (статус, кол-во записей, ошибки, время)

**3. NEW STORE AUTO-SYNC**:
- При создании магазина через POST `/api/businesses`:
  - Магазин создаётся в Supabase
  - Автоматически запускается `syncService.syncAllData()` в фоне
  - Не блокирует ответ пользователю
  - Данные за 90 дней загружаются в фоновом режиме
  - После завершения доступны во всех отчётах

**4. API ENDPOINTS REFACTORED**:
- `/api/wb-finance` → теперь читает из `db.getSalesFromCache(business_id, dateFrom, dateTo)`
- `/api/wb-sales` → читает из Supabase wb_sales (фильтр по sale_id)
- `/api/wb-orders` → читает из Supabase wb_orders
- `/api/wb-fin-report` → читает из Supabase wb_financial_reports
- Все endpoint'ы теперь **НЕ обращаются к WB API напрямую**
- Быстрые запросы, нет лимитов, работа с кэшированными данными

**5. MANUAL SYNC**:
- Добавлены endpoints:
  - POST `/api/sync/:businessId` — ручной запуск синхронизации
  - GET `/api/sync-status/:businessId` — статус последней синхронизации
- Кнопка "🔄 Ручное обновление" в интерфейсе
- Загружает данные за 90 дней, **исключая сегодня**

**6. BUGS FIXED**:
- Boolean comparison: `is_active === true || is_active === 1` для совместимости PostgreSQL/SQLite
- getBusinessById/getDefaultBusiness: добавлен await (теперь async)
- database.js: все функции с Supabase async (getSalesFromCache, getOrdersFromCache, getFinancialReportFromCache)

**7. FILES STRUCTURE**:
```
.env — Supabase credentials
supabase-client.js — клиент Supabase
sync-service.js — сервис синхронизации (432 строки)
supabase-schema.sql — SQL схема (7 таблиц)
database.js — полностью async (524 строки)
index.js — обновлены все endpoints (4153 строки)
```

**NEXT STEPS** (если понадобится):
- Тестирование полного цикла: создание магазина → авто-синхронизация → отображение данных
- Проверка cron job'а (можно подождать до 3:30 или изменить время для теста)
- Мониторинг логов синхронизации в таблице sync_logs

---

## Previous Session (Jan 13, 2026) - ✅ ROUTING RESTRUCTURE:
- **Major Change**: Финансовый отчет теперь главная страница сервиса
- **New Main Page**: `/` - финансовый отчет с управлением компаниями (ранее был `/fin-report`)
- **Products Page**: `/products` - анализ товаров MAX UI (ранее был `/`)
- **Navigation Updated**: 
  - На главной странице (`/`) добавлена кнопка "🔍 Анализ товаров" → `/products`
  - На странице `/products` кнопка "📈 Фин отчёт" заменена на "🔍 Главная" → `/`
- **Login Redirect**: После авторизации редирект на `/` (финансовый отчет)
- **Code Changes**:
  - `app.get('/')` теперь обслуживает финансовый отчет (index.js ~line 1340)
  - `app.get('/products')` обслуживает MAX UI для анализа товаров (index.js ~line 758)
  - Удалён старый маршрут `/fin-report`
- **Reason**: Финансовый отчет — основной функционал для пользователей, логично сделать его главной страницей
- **Status**: ✅ Server restarted, changes applied

Previous Session (Dec 23, 2025):
- **Issue Found**: Companies dropdown on `/fin-report` shows "Загрузка..." but doesn't populate
- **Root Cause**: `loadBusinesses()` function was defined but only called from `openBusinessManager()` modal, not on page initialization
- **Fix Applied**: Added `DOMContentLoaded` event listener before closing `</script>` tag to auto-call `loadBusinesses()` when page loads
- **Code Location**: index.js lines 2955-2962 (closing `/fin-report` route template)
- **Status**: ✅ Server restarted successfully - fix applied and ready for testing
- **Next Action**: Verify companies now load automatically in dropdown selector on page load

Last Session Completed (Dec 17, 2025 - Evening):
- ✅ New UI/UX workflow for financial reports (modal-based approach)
- ✅ Loading indicators with visual feedback (spinner + badges)
- ✅ Click outside modal to close functionality
- ✅ Data validation before opening reports
- ✅ Error handling improvements for async loading
- ✅ Button "ОБНОВИТЬ ДАННЫЕ" moved to top-right corner
- ✅ All report buttons converted to modal triggers with unique colors

Previous Session (Dec 17, 2025 - Afternoon):
- ✅ Multi-company aggregation for sales report ("Все активные компании" mode)
- ✅ Company name column added to sales report (first column)
- ✅ Sortable columns with visual indicators (↕ symbol, hover effects)
- ✅ Default sort by company name (alphabetically)
- ✅ Tabs system for financial report when multiple companies loaded
- ✅ Tab switching using numeric indices (avoids quote escaping issues)
- ✅ Default selector set to "All active companies" when multiple exist
- ✅ Improved tab design: flat style, color-coded, underline indicator

Current Focus:
- Modal-based report viewing system fully operational
- Loading state management with visual indicators
- Better UX for data fetching and error handling
- All 3 reports load in parallel when "ОБНОВИТЬ ДАННЫЕ" clicked

Recent Changes (Dec 17, 2025 - Evening):

**✅ COMPLETED - MODAL WORKFLOW & LOADING INDICATORS:**
- **NEW WORKFLOW**:
  - **"ОБНОВИТЬ ДАННЫЕ" button**: Single button loads all 3 reports at once (parallel loading)
  - **Report buttons**: Now open modals instead of switching views (📈 Фин отчёт, 💰 Продажи, 📦 Заказы)
  - **Button positioning**: "ОБНОВИТЬ ДАННЫЕ" moved to top-right corner with purple style matching other nav buttons
  - **Color-coded buttons**: Each report button has unique gradient (purple, pink, cyan)
- **LOADING INDICATORS**:
  - **Main loading block**: Shows below buttons with animated spinner during data fetch
  - **Badge indicators**: Small ⏳ badges appear on each report button while loading
  - **Progressive completion**: Each badge disappears when its report finishes loading
  - **Auto-hide**: Main loading block hides when all 3 reports complete
  - **CSS animations**: `@keyframes spin` for spinner, `@keyframes pulse` for badges
- **DATA VALIDATION**:
  - **Global flags**: `finReportDataLoaded`, `salesReportDataLoaded`, `ordersDataLoaded`
  - **Empty state message**: "Данные не загружены" (simplified, gray text) when opening unloaded report
  - **Flag reset**: All flags reset to `false` when "ОБНОВИТЬ ДАННЫЕ" clicked
  - **Error handling**: Flags set to `true` even on errors/empty data to prevent infinite loading
- **MODAL IMPROVEMENTS**:
  - **Click outside to close**: Click on modal backdrop closes the modal (using `onclick` with `event.stopPropagation()`)
  - **Modal structure**: Each modal has `onclick` handler, inner content has `stopPropagation()`
  - **Function**: `closeModalOnOutsideClick(event, modalId)` checks if click target is backdrop
- **ERROR HANDLING**:
  - **All fetch operations**: Set loading flags even on error/catch blocks
  - **Empty data**: Set loading flags when `data.items` is empty or null
  - **Prevents infinite wait**: Ensures loading indicators always complete
- **CODE LOCATIONS** (index.js):
  - Lines 1355-1385: Main loading indicator HTML + button badges
  - Lines 1598-1602: Global loading flags declaration
  - Lines 2192-2212: `loadFinancialData()` - resets flags, shows indicators, loads all reports
  - Lines 2214-2223: `checkAllDataLoaded()` - hides indicators when all complete
  - Lines 2225-2255: Modal open functions with data validation
  - Lines 2290-2335: `loadOrders()` with error flag handling
  - Lines 2421-2460: `loadSalesReport()` with error flag handling
  - Lines 2655-2707: `loadFullFinReport()` with error flag handling
  - CSS animations: `@keyframes spin` and `@keyframes pulse` for visual effects

Recent Changes (Dec 17, 2025 - Afternoon):
  - **Console logging**: Added debugging logs for data flow tracking (to be removed later)
  - **Error prevention**: Removed getElementById('datasetBody') causing null reference errors
- **CODE LOCATIONS** (index.js):
  - Lines 2372-2540: `displaySalesReport()` - renders sales with company column, sorting, aggregation
  - Lines 2480-2547: `sortSalesReport()` - handles column sorting with state management
  - Lines 2549-2590: `loadFullFinReport()` - loads financial data, supports 'all' mode
  - Lines 2607-2682: `displayFullFinReport()`, `switchFinReportCompany()`, `highlightActiveFinTab()` - tab system
  - Lines 2684-2750: `renderFinReportData()` - renders financial report table
  - Lines 1803-1848: `updateBusinessSelector()` - defaults to 'all' when multiple companies

Recent Changes (Dec 7, 2025 - Late Evening):

**✅ COMPLETED - PRODUCT COST MANAGEMENT (СЕБЕСТОИМОСТЬ):**
- **NEW BUTTON**: "💰 Себестоимость" button added after "📦 Заказы" button
- **NEW MODAL**: Dedicated modal for product cost management
- **NEW DB TABLE**: `product_costs` (business_id, nm_id, subject, brand, cost)
- **CASCADE DELETE**: Removing business deletes all its product costs
- **DATABASE FUNCTIONS**:
  - `upsertProductCost()` - save/update single product cost
  - `bulkUpsertProductCosts()` - batch save multiple costs
  - `getProductCostsByBusiness()` - load all costs for a business
  - `getProductCost()` - get cost for specific product
  - `deleteProductCost()` - remove product cost
- **API ENDPOINTS**:
  - GET `/api/product-costs/:businessId` - list all costs
  - POST `/api/product-costs/:businessId/bulk` - bulk save costs
  - GET `/api/product-costs/:businessId/:nmId` - get single cost
  - DELETE `/api/product-costs/:businessId/:nmId` - delete cost
- **UI FEATURES**:
  - Modal with fixed size (max-width: 1000px, max-height: 80vh)
  - Internal scrolling for large datasets
  - "🚀 Запустить загрузку" button to fetch products from WB API
  - Products auto-load from sales data (via `/api/wb-sales-grouped`)
  - "💾 Сохранить" button to persist costs to database
  - 4 columns: Артикул WB, Предмет, Бренд, Себестоимость (₽)
  - Manual cost input per product (number input field)
  - Auto-load saved costs when opening modal
  - Costs persist across sessions per business
- **DATA FLOW**:
  - Modal uses parent's `currentBusinessId` for API calls
  - No API key stored in modal itself
  - Fetches product list via sales endpoint
  - Merges saved costs from DB with fresh product data
  - Each business has independent cost tracking

**✅ COMPLETED - DATABASE & MULTI-COMPANY ARCHITECTURE:**
- **DATABASE**: SQLite with `better-sqlite3` package
- **TABLES**: 
  - `accounts` (id, username, password_hash, email, timestamps)
  - `businesses` (id, account_id, company_name, wb_api_key, description, is_active, timestamps)
- **RELATIONS**: CASCADE DELETE on account removal (deletes all businesses)
- **SECURITY**: Password hashing with SHA256 + salt (pbkdf2)
- **MIGRATION**: Auto-migration from `wb-api-key.txt` to database on first run
- **DEFAULT ACCOUNT**: Created automatically: admin / tarelkastakan

**✅ COMPLETED - AUTHENTICATION SYSTEM:**
- **OLD SYSTEM REMOVED**: Static ADMIN_LOGIN/ADMIN_PASSWORD constants deleted
- **NEW SYSTEM**: Database-driven authentication via `database.js` module
- **LOGIN**: POST `/api/login` returns account ID as token (stored in httpOnly cookie)
- **MIDDLEWARE**: `requireAuth` checks cookie token and loads account from DB
- **SESSION**: Account object attached to `req.account` on each request

**✅ COMPLETED - BUSINESS MANAGEMENT UI:**
- **OLD MODAL REMOVED**: "Добавить API ключ" button and modal deleted
- **NEW UI**: "🏢 Управление компаниями" button opens business manager modal
- **FEATURES**:
  - List all companies with status badges (active/inactive)
  - Add new company with form (name, API key, description)
  - Toggle active/inactive status per company
  - Delete company with confirmation
  - Company selector dropdown in header
  - Auto-load first active company on page load

**✅ COMPLETED - FINANCIAL REPORTS WITH MULTI-COMPANY:**
- **PAGE**: `/fin-report` - financial dashboard per selected company
- **API ENDPOINTS UPDATED**:
  - `/api/wb-finance?businessId=X` - financial data for specific company
  - `/api/wb-sales?businessId=X` - sales for specific company
  - `/api/wb-orders?businessId=X` - orders for specific company
  - `/api/wb-sales-grouped?businessId=X` - grouped sales
  - `/api/wb-fin-report?businessId=X` - full WB financial report (82 columns)
- **BUSINESS APIS**:
  - GET `/api/businesses` - list all companies of current account
  - POST `/api/businesses` - create new company
  - PUT `/api/businesses/:id` - update company (name, key, status)
  - DELETE `/api/businesses/:id` - delete company
  - GET `/api/businesses/default` - get first active company
- **REPORT TYPES**: 
  - 📈 Financial Report (82 columns - full WB report structure)
  - 💰 Sales Report (10 columns - grouped by unique articles)
- **UI FEATURES**:
  - Company selector with auto-switch
  - Business manager modal with CRUD operations
  - All load functions check `currentBusinessId` before API calls
  - Date range selector (custom periods)
  - Toggle buttons for report types
  - 5 dashboard cards (revenue, commission, logistics, profits)
  - Dynamic table headers (82 vs 10 columns)
  - Sticky header on scroll

Recent Changes (Dec 2, 2025):

**LATEST UPDATE - AUTH FIX FOR VERCEL:**
- **CRITICAL FIX**: Авторизация через httpOnly cookies (работает на serverless)
- **PACKAGE**: Добавлен `cookie-parser` для работы с cookies
- **LOGIN FLOW**: POST /api/login устанавливает cookie с base64 токеном
- **MIDDLEWARE**: requireAuth проверяет cookie (приоритет) и Authorization header (fallback)
- **SECURITY**: httpOnly=true, secure в production, sameSite=lax, maxAge=24h
- **TESTED LOCALLY**: ✅ Все тесты прошли перед деплоем
- **RESULT**: Стабильная авторизация на Vercel без проблем с сессиями

**UPDATE - CATEGORY PARSING FROM API:**
- **CRITICAL CHANGE**: Категории теперь парсятся из поля `product.entity` (API v2)
- **DATA SOURCE**: Поле `entity` содержит название категории на русском языке
- **REMOVED**: Удалён статичный маппинг `subjectId → название` (40+ строк кода)
- **EXAMPLES**: "кроссовки" → "Кроссовки", "юбки" → "Юбки", "зонты пляжные" → "Зонты пляжные"
- **FORMATTING**: Первая буква автоматически становится заглавной для красоты
- **BENEFIT**: Автоматическое покрытие ВСЕХ категорий WB без ручного маппинга
- **PERFORMANCE**: Нулевые накладные расходы, данные уже в API
- **CSV**: Обновлены оба endpoint (/wb-max и /wb-max-csv)

**LATEST UPDATE - SIMPLIFIED SELLER DATA:**
- **DECISION**: Отключен парсинг юридических лиц (WB блокирует все методы)
- **CURRENT STATE**: Показываем только sellerId + storeName (торговое название из API)
- **TWO COLUMNS**: 
  1. `Продавец (ID)` - показывает только ID продавца (например "ID: 1399211")
  2. `Магазин` - торговое название из `product.supplier` (например "Мариям")
- **REASON**: Wildberries блокирует парсинг (код 498, капча, анти-бот защита)
- **FUTURE**: Для полных юрлиц требуется Puppeteer/Selenium на отдельном VPS
- **ARCHITECTURE**: Нет статической базы продавцов - используется только API данные
- **PERFORMANCE**: Быстрая загрузка ~1-3 сек без попыток парсинга

Previous Changes:
- **NEW FEATURE**: Добавлены колонки "Категория" и "Цвет" между Продавцом и Ценой
- **DATA SOURCE**: Категория извлекается из `product.subjectId` с маппингом на названия (40+ категорий)
- **DATA SOURCE**: Цвет извлекается из `product.colors[0].name` (берем ПЕРВЫЙ цвет - основной для данного артикула)
- **FIX**: Цвет теперь показывает только один цвет товара, а не все через запятую
- **MAPPING**: Расширенный маппинг категорий: одежда, обувь, электроника, дом, красота, детям, авто
- **MAJOR CHANGE**: Убрано ограничение на ИП - теперь показываем ВСЕХ продавцов
- **UI CHANGE**: Колонка "Продавец (ID)" показывает: `Название продавца (ID продавца)`
- **SIMPLIFICATION**: Убрано поле `sellerName` - теперь используется только `storeName` из `product.supplier`
- **SIMPLIFICATION**: Удалена проверка на ИП перед парсингом - берём название из API для всех
- **REMOVED**: Удалена статическая база продавцов (sellers-db.json) - не нужна
- **CSV UPDATE**: Обновлён формат CSV - добавлены `category` и `color`, убрана `sellerName`
- **CRITICAL FIX**: Удалён медленный `fetchStoreNameFromProductPage` который делал 3+ HTTP-запроса
- **CRITICAL FIX**: Упрощён `extractPrice` - убраны дублирующиеся проверки, оставлены только рабочие поля
- **CRITICAL FIX**: Удалено дублирование функции `summarizeStocks` (была определена дважды)
- **CRITICAL FIX**: Добавлены недостающие функции `safeGet` и `currencyByDomain` в wb-max-csv
- **UI FIX**: Исправлена проверка цены - теперь `price !== null && price > 0` вместо `typeof price === 'number'`
- **UI FIX**: Исправлены индексы innerHTML - правильно отображаются фото, склады и статус
- **UI FIX**: Товары без остатков показывают "нет в наличии" вместо "0.00"
- **CRITICAL FIX**: Название магазина берётся из `product.supplier` в API v2 (не требует парсинга!)
- Результат: загрузка **~2-8 сек** (с учетом парсинга), показываем полные юрлица или краткие названия
- UI: артикул — ссылка на KG
- UI: колонка `Склады` показывает `Название — N шт`, модель FBO/FBS

Next Steps:
- Добавить кэширование часто запрашиваемых товаров (опционально)
- Расширять словарь `wh → название` по наблюдениям

Decisions:
- Сохранять `/wb-price-csv` публичным и минимальным.
- HTML парсинг имени продавца использовать только для JSON/UI, не утяжеляя CSV.
- В UI поддерживать простоту чтения через раздельные колонки.
- Данные по складам считаются из публичного `sizes[].stocks` (агрегация по `wh`).
- Для аналитики конкурентов используем только публичные данные WB (без приватных ключей).

Preferences:
- Minimal public endpoints; robust fallbacks; readable UI.

Learnings:
- Wildberries APIs can vary; layered fallbacks are essential.
- Sheets `IMPORTDATA` returns headers+data; use `INDEX` to select cells.
